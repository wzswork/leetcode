/**
 * @param {number[]} nums
 * @return {number[][]}
 */
function generate(){
    num1.concat(num2[i])
    res.push(num1)
}

var permute = function(nums) {
    let res = [];
    for(let i = 0; i<nums.length; i++){
        res.push(nums[i])
    }
};

let nums = [1,2,3];

console.log(permute(nums));